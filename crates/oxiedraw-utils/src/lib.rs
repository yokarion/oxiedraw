//! Workspace utilities.
//!
//! Pure helpers and shared primitives reusable across the workspace.
//! This crate has no workspace dependencies; both `oxiedraw-core` and
//! `oxiedraw-ui` depend on it.

pub mod color;
pub mod geometry;
pub mod math;
pub mod pixels;
pub mod tracing;
