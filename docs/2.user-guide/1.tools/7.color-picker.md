---
title: Color Picker
description: Sample a color from the canvas.
---

# Color Picker

The Color Picker (eyedropper) sets the primary color by sampling a pixel from
the canvas. It reads the composited result under the cursor, so you can pick up
a blended color, not just the raw layer value.
